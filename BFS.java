public class BFS {
    public static void search(Board init, Board end) {
        if (Board.isSolution(init, end)) System.out.println("It's possible, but I don't have the procedure, yet...");
        else System.out.println("That's impossible");
    }
}
